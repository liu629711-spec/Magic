# @magic/dsh-engineering

Magic engineering-organization plugin for DeepSeek Harness.

It contributes a system-prompt section and an `/engineering` command. Engineering is created only by `/engineering confirm <name>`. CEO mode never creates it.
