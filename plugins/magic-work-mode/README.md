# @magic/dsh-work-mode

Magic agent / CEO work-mode plugin for DeepSeek Harness.

It contributes a system-prompt section and a `/mode` command. Session default is agent. `/mode ceo` changes the session default; it does not create an engineering organization.
