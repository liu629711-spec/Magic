{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Magic desktop runtime and window permissions.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
