[package]
name = "magic-application"
version.workspace = true
edition.workspace = true

[dependencies]
magic-contracts = { path = "../contracts" }
magic-domain = { path = "../domain" }
magic-execution-port = { path = "../execution-port" }
magic-persistence-port = { path = "../persistence-port" }
serde_json = "1"
sha2 = "0.10"
thiserror.workspace = true
uuid = { version = "1", features = ["v4"] }

[dev-dependencies]
magic-persistence = { path = "../persistence" }
