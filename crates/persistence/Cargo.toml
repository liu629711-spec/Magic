[package]
name = "magic-persistence"
version.workspace = true
edition.workspace = true

[dependencies]
magic-domain = { path = "../domain" }
magic-persistence-port = { path = "../persistence-port" }
rusqlite = { version = "0.40", features = ["bundled"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
