[package]
name = "magic-tauri-desktop-runtime-spike"
version = "0.1.0"
description = "Isolated runtime spike for Magic"
authors = ["Magic"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = { version = "2", features = [] }
