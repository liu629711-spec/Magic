{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Only the fixed runtime commands required by the spike.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
