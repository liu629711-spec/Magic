# AI 核心总监交接

## 交接范围

负责 Agent 运行、上下文、记忆、编排、工具、模型调用和恢复语义的核心技术判断，维护产品语义与运行时事实之间的边界。

## 必读材料

- `D:\Harmess\reference-project\AgentCore\AGENTCORE_EXISTING_AI_CORE_BREAKDOWN.md`
- `D:\Harmess\Magic\docs\01-产品\会议记录-第三轮-AI核心总监.md`
- `D:\Harmess\Magic\docs\01-产品\核心对象模型.md`

## 已交接结论

- 所有 Agent 是完整 Agent；临时、会话成员、项目成员和借调成员描述关系与边界，不描述能力等级。
- 角色、权限、归属、可见范围、责任和生命周期不可混为一个概念。
- 私有记忆、完整 transcript、文件、结果包、产物和共享事实必须分层；协作核心是受控回传而不是完整会话共享。

## 首要工作

1. 定义 AgentIdentity、成员关系、任务上下文包和结果包的最小契约。
2. 明确 TaskAttempt 的上下文构建、工具调用、停止、恢复和副作用记录边界。
3. 设计 CEO/临时协作者与工程 PM/成员之间的编排责任分工。
4. 定义最小可审计的记忆、事实确认、撤销和来源策略。
5. 参与 F-10，验证重试、恢复和副作用后 AI 侧状态不会伪造任务成功。

## 不得外推

运行时的 CAPTAIN、worker 或上下文续接不能自动成为长期成员、PM 或跨工程共享记忆。
