# Magic Domain

Magic 是面向真实交付的 Agent 工作台。本文件只统一产品和领域词汇，不记录运行时、数据库或框架实现。

当前实现形态：Magic 是 DSH 之上的产品插件层。DSH 负责执行；Magic 插件负责工作方式和长期组织。

## 工作对象

**项目（Project）**：产品侧的容器，下面可以管理多个工作区和会话。
_Avoid_: 把项目当作 Agent 实际工作的目录

**工作区（Workspace）**：项目下的一个本地工作目录（文件夹），是 Agent 读取、修改和运行文件的地方。
_Avoid_: 项目、工程

**会话（Session）**：围绕一项连续工作的交流和执行现场。
_Avoid_: 运行、任务

**任务（Task）**：有目标、责任人、验收标准和最终结果的一项工作。
_Avoid_: 消息、回合

**计划步骤（Plan Step）**：任务中的普通执行步骤，不一定需要独立 Agent 或独立交付。
_Avoid_: 子任务

**子任务（Child Task）**：需要独立责任、交付、验收、等待或依赖关系的任务。
_Avoid_: 计划步骤

**执行尝试（Attempt）**：针对一个 Task 实际发起的一次执行过程；重试会产生新的 Attempt。
_Avoid_: 回合、Session、Run

**底层绑定（Execution Binding）**：Attempt 与具体执行底座会话、输入、进程或事件引用之间的关系。
_Avoid_: Run 绑定（除非底座实际提供该对象）

**执行底座（Execution Base）**：提供会话、模型、工具和执行能力的外部 Agent 运行时；当前为 DSH。
_Avoid_: Magic 产品层

## 工作方式与组织

**代理（Agent mode）**：当前会话主 Agent 直接理解、计划、执行、验证和交付。
_Avoid_: 把内部临时协作者当成 CEO 或工程成员

**CEO（CEO mode）**：当前会话主 Agent 显式组织协作、依赖、汇报和统一交付。
_Avoid_: 永久组织、工程、文件夹

**工程（Engineering）**：用户确认后建立的长期组织，拥有 PM、成员、任务账本和项目事实。
_Avoid_: 长聊天、多 Agent、CEO 模式的别名

## 结果与治理

**产物（Artifact）**：用户可以访问、验证并使用的交付结果。
_Avoid_: 回复、输出

**事实（Fact）**：工程认可、带来源和版本的可复用信息。
_Avoid_: 记忆、摘要

**副作用（Side Effect）**：文件写入、命令执行、费用、网络请求、发布或其他真实影响。
_Avoid_: 普通消息

**验收（Acceptance）**：责任人依据预先定义的标准和证据确认 Task 是否完成。
_Avoid_: 模型成功、请求成功

**状态未知（Unknown）**：已有证据不足以判断结果的诚实状态，不等于失败或成功。
_Avoid_: 空闲、异常

**幂等键（Idempotency Key）**：客户端或 Magic 为一次有副作用请求提供的稳定标识，用于避免重复执行。
_Avoid_: 底层消息 ID（它只是底层引用）
